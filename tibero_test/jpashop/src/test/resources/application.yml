spring: #test에서는 메모리 db를 사용하는 것으로 바꾸어봄. 티베로는 안 될 듯?
#스프링부트는 기본적으로 메모리 모드로 돌리기 때문에 아래 다 없어도 됩니다!
#  datasource:
#
#    url: jdbc:h2:mem:test #메모리모드!  #jdbc:h2:tcp://localhost/~/jpashop
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create #create-drop #기본적으로 스프링부트는 크레이트드랍. 마지막에 드랍쿼리 다 날려줌
#    properties:
#      hibernate:
#        #show_sql: true
#      format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace